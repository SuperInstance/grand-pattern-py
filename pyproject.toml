[project]
name = "grand-pattern"
version = "0.1.0"
description = "Grand Pattern toolkit — cellular graph intelligence in pure Python"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"
